<h1>Mis Propiedades: </h1>

<% if user_signed_in? %>
    <%= link_to 'Agregar Propiedad', new_property_path %>
<% end %>

<%= will_paginate @properties %>

<div id="properties-grid">
  <% @properties.each do |property| %>
    <%= render 'card', property: property %>
  <% end %>
</div>

<%= will_paginate @properties %>